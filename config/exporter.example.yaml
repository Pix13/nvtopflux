influxdb:
  url: "http://localhost:8086"
  token: "${INFLUXDB_TOKEN}"
  org: "gpu-monitoring"
  bucket: "nvtop"

nvtop:
  command: ["nvtop", "-s", "-l"]
  interval_seconds: 1

exporter:
  hostname_tag: true
  flush_interval_ms: 1000
  log_level: "INFO"
